/*
Cree una Base de datos de manera de forma en nube con postgresql

Se creo una tabla Users y Products con ID autoincrementable.
Users con los campos de usuario (username) y contraseña (password)
Products con los campos nombre (name), categoria (category), precio (price), cantidad (stock), y estado (state)

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    category VARCHAR(50) NOT NULL,
    price NUMERIC(10, 2) NOT NULL,
    stock INT NOT NULL,
    state CHAR(1)
);

*/